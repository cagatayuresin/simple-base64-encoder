## 2024-08-04 - Adding ARIA labels to icon-only buttons
**Learning:** Found multiple icon-only buttons that were lacking ARIA labels, creating accessibility issues. The static HTML buttons lacked labels, as well as the dynamic buttons rendered via JS.
**Action:** When working on projects, remember to search for both static HTML and dynamic JS rendered elements to add `aria-label` tags for screen reader accessibility.
